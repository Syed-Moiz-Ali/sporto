name: referee_app
description: SPORTO Referee Mobile Application (Official Umpires & Scorekeepers)
version: 1.0.0+1
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  core:
    path: ../../packages/core
  ui_kit:
    path: ../../packages/ui_kit
  shared_domain:
    path: ../../packages/shared_domain
  referee_data:
    path: ../../packages/referee_data
  flutter_bloc: ^8.1.3
  equatable: ^2.0.5
  confetti: ^0.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

