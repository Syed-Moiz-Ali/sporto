import 'package:flutter/material.dart';

abstract final class SportoScoringTokens {
  // ============================================================
  // REFERENCE
  // ============================================================

  static const double designWidth = 390;

  static const double pagePadding = 20;
  static const double headerHeight = 36;

  // ============================================================
  // SCREEN BACKGROUND
  // ============================================================

  static const Color backgroundTop = Color(0xFF131924);
  static const Color backgroundMiddle = Color(0xFF10141E);
  static const Color backgroundBottom = Color(0xFF0E0C08);

  // ============================================================
  // MATCH HEADER CARD
  // ============================================================

  static const Color matchStart = Color(0xFF19222D);
  static const Color matchMiddle = Color(0xFF2A2028);
  static const Color matchEnd = Color(0xFF1A2024);

  // ============================================================
  // SCORE CARD
  // ============================================================

  static const Color scoreStart = Color(0xFF56332C);
  static const Color scoreMiddle = Color(0xFF073D2A);
  static const Color scoreEnd = Color(0xFF46530D);

  // ============================================================
  // INPUT / SELECTION
  // ============================================================

  static const Color inputSurface = Color(0xFF161A24);
  static const Color inputBorder = Color(0xFF29405E);

  // ============================================================
  // COMPLETED
  // ============================================================

  static const Color inningsSurface = Color(0xFF15281B);
  static const Color inningsBorder = Color(0xFF245538);

  // ============================================================
  // TIE BREAKER
  // ============================================================

  static const Color tieStart = Color(0xFF241B12);
  static const Color tieEnd = Color(0xFF252319);
  static const Color tieBorder = Color(0xFF6B5310);

  // ============================================================
  // SEMANTIC
  // ============================================================

  static const Color orange = Color(0xFFFF9800);
  static const Color green = Color(0xFF36D58B);
  static const Color blue = Color(0xFF53C4F2);
  static const Color red = Color(0xFFFF5257);

  // ============================================================
  // BALLS
  // ============================================================

  static const Color ballBlue = Color(0xFF53C4F2);
  static const Color ballNeutral = Color(0xFF29314A);
  static const Color ballSix = Color(0xFFFF68B8);

  // ============================================================
  // WINNER
  // ============================================================

  static const Color winnerStart = Color(0xFF4A2924);
  static const Color winnerMiddle = Color(0xFF063A27);
  static const Color winnerEnd = Color(0xFF39460A);
}
