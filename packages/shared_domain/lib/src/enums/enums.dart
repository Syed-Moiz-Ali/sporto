enum SportType { cricket, football, basketball, tennis, badminton }

enum SyncStatus { synced, pendingSync, syncFailed }

enum MatchStatus { upcoming, verification, toss, live, completed }

enum TossChoice { bat, bowl }

enum WicketType { bowled, caught, runOut, lbw, stumped, hitWicket, retiredHurt }
